# Domain Docs

This is a single-context repository.

## Before exploring

Read the root `CONTEXT.md` and relevant records under `docs/adr/` when they exist. Proceed silently when either is absent.

## Vocabulary

Use terms exactly as defined by `CONTEXT.md`. If a needed concept is absent, reconsider whether the term belongs or note the gap for later domain modeling.

## Architecture decisions

Surface conflicts with an existing ADR explicitly rather than silently overriding it.

## Expected layout

```text
/
├── CONTEXT.md
├── docs/adr/
└── src/
```
